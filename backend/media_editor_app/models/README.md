# rembg model files

Place `u2net.onnx` in this folder for **Remove background**.

Download (on the host, not inside Docker):

https://github.com/danielgatis/rembg/releases/download/v0.0.0/u2net.onnx

Docker cannot download this reliably (GitHub SSL failures), so the file must exist locally before using the feature.
